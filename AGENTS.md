# Project Agent Guidance

- Adopt and follow the rules defined in `./.cursor/rules` as the primary project guidance.
- Refer to `agents/` for internal research notes and build-specific documentation relevant to the current implementation.
- Treat `agents/spec/` as the most specific and authoritative location within `agents/` for research notes and implementation documentation.
- Treat other documentation under `agents/` as ancillary supporting material, including folders such as `agents/audit-resolutions/`.
